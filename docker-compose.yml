version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: http://localhost:8080/api/v1/leads
    container_name: lead_qualifier_frontend
    restart: always
    ports:
      - "3000:80"
